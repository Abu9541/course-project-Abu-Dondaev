# Статический анализ кода (Этап 6)

## 1. Инструменты и методика

Для контроля качества кода применяется статический анализ:

- **Сервер (Java):** Checkstyle / SonarLint (IDE), при необходимости — SonarQube
  (полноценный анализатор с метриками сложности, дублирования, уязвимостей).
- **Клиент (Kotlin):** встроенные инспекции Android Studio + `Android Lint` + `detekt`
  (рекомендуется для Kotlin).

Цель — выявить и устранить: «code smells», цикломатическую сложность, дублирование,
потенциальные NPE, нарушения соглашений именования и неиспользуемый код.

## 2. Как сформировать отчёт

**Android Lint (без доп. настройки):**
```bash
cd application/android
./gradlew lint        # отчёт: app/build/reports/lint-results-debug.html
```

**SonarQube (опционально, локально через Docker):**
```bash
docker run -d --name sonarqube -p 9000:9000 sonarqube:lts-community
# затем в backend: mvn sonar:sonar -Dsonar.host.url=http://localhost:9000 -Dsonar.login=<token>
```

> При сдаче приложить скриншоты сводки в `docs/images/` (например,
> `sonar-overview.png`, `android-lint.png`) и краткое резюме исправленных замечаний.

## 3. Качество, заложенное на этапе разработки

Ряд типичных замечаний предотвращён архитектурно ещё до анализа:

| Категория | Мера |
|-----------|------|
| Дублирование доступа к данным | Spring Data репозитории (нет рукописного CRUD-SQL) |
| «Толстые» контроллеры | Тонкий слой Control, логика — в Mediator |
| Магические значения | Перечисления (`enum`) для статусов/типов |
| Сложные ветвления | Конечные автоматы в сущностях (`Order`, `Vehicle`) |
| Обработка ошибок «по месту» | Единый `@RestControllerAdvice` (`GlobalExceptionHandler`) |
| Ручное преобразование объектов | Слой `mapper` (Data Mapper) |
| Жёсткие зависимости | Внедрение зависимостей через интерфейсы |
| Хранение чувствительных данных | Пароли — BCrypt; карта — только маска; токен — DataStore |

## 4. Типовые замечания и их статус (пример сводки)

| Замечание | Серьёзность | Статус |
|-----------|-------------|--------|
| Неиспользуемые импорты | Minor | Устранены |
| Длинные методы (> 50 строк) | Major | Декомпозированы (выделены приватные хелперы в сервисах) |
| Отсутствие валидации входных DTO | Major | Закрыто Bean Validation (`@Valid`, аннотации на полях) |
| Возможный NPE при сетевом ответе | Major | Закрыто `safeApiCall` + `Result` на клиенте |
| Хардкод базового URL | Minor | Вынесен в `BuildConfig.BASE_URL` |

## 5. Рефакторинги, выполненные по ходу проекта

- Вынесение бизнес-логики из контроллеров в сервисы (соблюдение PCMEF).
- Замена «анемичных» сущностей на сущности с бизнес-методами.
- Централизация рассылки уведомлений персоналу (`notifyManagers`) вместо дублирования.
- Единая обёртка сетевых вызовов клиента (`safeApiCall`) с типобезопасной обработкой ошибок.
- Переиспользуемые UI-компоненты (`FilterDropdown`, `VehicleCard`, `FullScreen*`).
