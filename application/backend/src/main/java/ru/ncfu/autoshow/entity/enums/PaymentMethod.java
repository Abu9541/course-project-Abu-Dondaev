package ru.ncfu.autoshow.entity.enums;

/** Способ оплаты заказа. */
public enum PaymentMethod {
    /** Оплата банковской картой (через имитацию платёжного шлюза). */
    CARD,
    /** Оплата наличными в салоне (отмечает менеджер). */
    CASH
}
