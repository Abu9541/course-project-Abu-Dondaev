package ru.ncfu.autoshow.entity.enums;

/** Роли пользователей системы (разграничение доступа). */
public enum Role {
    /** Клиент автосалона — просмотр каталога, заявки, тест-драйвы, покупки. */
    CLIENT,
    /** Менеджер по продажам — обработка заявок, тест-драйвов и заказов. */
    MANAGER,
    /** Администратор — управление каталогом, пользователями и системой. */
    ADMIN
}
